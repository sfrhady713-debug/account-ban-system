#!/usr/bin/env python
# -*- coding: utf-8 -*-
"""
سیستم فیلتری پیشرفته برای سروش
فیلترینگ پیام‌ها، تماس‌ها، و رفتار مشکوک
"""

import re
from datetime import datetime
from typing import Dict, List, Tuple

class SorushMessageFilter:
    """فیلتری پیام‌های سروش"""
    
    def __init__(self):
        self.bad_words_fa = {
            'فحاشی': [
                'گاییدم', 'گایید', 'کوفت', 'کجا', 'الاغ', 'خمار',
                'بیشرف', 'حقیر', 'احمق', 'ابله', 'سوسول', 'روسپی',
                'فاسق', 'فاجر', 'شنگول', 'یاغی', 'خائن', 'دغل',
                'هرزه', 'سگ', 'خنزیر', 'لعنت', 'نفرین', 'سوگند',
                'کسخل', 'آشغال', 'حرام‌زاده', 'بنده‌اهو', 'ساکت',
            ],
            'تهدید': [
                'می‌کشم', 'میکشم', 'می‌ضربم', 'میضربم', 'می‌کنم تو رو',
                'میکنم تو رو', 'پاره کنم', 'می‌ترومندم', 'بی‌سلام',
                'از بین برم', 'نابود کنم', 'خونریزی', 'جنازه',
                'شکستن استخوان', 'کشتار', 'انفجار', 'بمب',
            ],
            'جاسوسی': [
                'رمز', 'پسورد', 'password', 'اطلاعات شخصی',
                'شماره تلفن', 'آدرس', 'شماره کارت', 'شناسه',
                'توکن', 'کلید خصوصی', 'حساب بانکی', 'شناسنامه',
                'کد ملی', 'پیامک تائید', 'کد ورود',
            ],
            'اسپم': [
                'خریدید', 'فروش', 'تخفیف', 'محدود', 'فقط امروز',
                'شانس', 'برنده', 'جایزه', 'رایگان', 'کلیک کنید',
                'روابط عمومی', 'بنر تبلیغی', 'کمپین',
            ],
            'محتوای عنف': [\n                'خشونت', 'زخم', 'خون', 'قتل', 'اسلحه',\n                'تیراندازی', 'سقوط', 'تصادف', 'مرگ',\n            ]\n        }\n        \n        self.suspicious_patterns = {\n            'تکرار بیش‌ازحد': (r'(.)\\1{4,}', 15),\n            'کپسلوک': (r'[A-Z]{5,}', 10),\n            'علائم بیش‌ازحد': (r'[!@#$%^&*]{3,}', 12),\n            'لینک مشکوک': (r'(http|https|bit\\.ly|tinyurl)', 20),\n            'شماره‌های متوالی': (r'\\d{10,}', 15),\n            'ایموجی بیش‌ازحد': (r'[😀-🙏]{5,}', 8),\n        }\n        \n        self.risk_scores = {\n            'فحاشی': 10,\n            'تهدید': 25,\n            'جاسوسی': 20,\n            'اسپم': 5,\n            'محتوای عنف': 30,\n            'رفتار مشکوک': 15,\n        }\n    \n    def check_message(self, message: str, username: str = None) -> Dict:\n        \"\"\"بررسی پیام برای تخلفات\"\"\"\n        violations = []\n        total_risk = 0\n        \n        # بررسی کلمات بد\n        for category, words in self.bad_words_fa.items():\n            for word in words:\n                if self._word_in_message(word, message):\n                    risk = self.risk_scores.get(category, 0)\n                    violations.append({\n                        'type': category,\n                        'word': word,\n                        'risk_score': risk,\n                        'severity': self._get_severity(risk)\n                    })\n                    total_risk += risk\n        \n        # بررسی الگوهای مشکوک\n        for pattern_name, (pattern, risk) in self.suspicious_patterns.items():\n            if re.search(pattern, message):\n                violations.append({\n                    'type': 'رفتار مشکوک',\n                    'pattern': pattern_name,\n                    'risk_score': risk,\n                    'severity': self._get_severity(risk)\n                })\n                total_risk += risk\n        \n        return {\n            'username': username,\n            'message': message,\n            'timestamp': datetime.now().isoformat(),\n            'has_violation': len(violations) > 0,\n            'violations': violations,\n            'total_risk': total_risk,\n            'risk_level': self._get_risk_level(total_risk),\n            'action': self._get_action(total_risk),\n            'is_banned': total_risk >= 100\n        }\n    \n    def _word_in_message(self, word: str, message: str) -> bool:\n        \"\"\"بررسی وجود کلمه در پیام\"\"\"\n        message_normalized = message.strip().lower()\n        word_normalized = word.strip().lower()\n        pattern = r'\\b' + re.escape(word_normalized) + r'\\b'\n        return bool(re.search(pattern, message_normalized))\n    \n    def _get_severity(self, risk: int) -> str:\n        \"\"\"تعیین شدت تخلف\"\"\"\n        if risk <= 5:\n            return 'کم'\n        elif risk <= 15:\n            return 'متوسط'\n        elif risk <= 25:\n            return 'بالا'\n        else:\n            return 'بسیار بالا'\n    \n    def _get_risk_level(self, score: int) -> str:\n        \"\"\"تعیین سطح خطر کلی\"\"\"\n        if score == 0:\n            return 'سالم'\n        elif score <= 10:\n            return 'کم'\n        elif score <= 30:\n            return 'متوسط'\n        elif score <= 60:\n            return 'بالا'\n        else:\n            return 'بسیار بالا'\n    \n    def _get_action(self, score: int) -> str:\n        \"\"\"تعیین عمل لازم\"\"\"\n        if score == 0:\n            return 'قبول'\n        elif score <= 10:\n            return 'هشدار'\n        elif score <= 30:\n            return 'محدود کردن'\n        elif score < 100:\n            return 'تعلیق موقت'\n        else:\n            return 'بن فوری'\n\n\nclass SorushCallFilter:\n    \"\"\"فیلتری تماس‌های سروش\"\"\"\n    \n    def __init__(self):\n        self.max_calls_per_hour = 50\n        self.max_call_duration = 3600  # 1 ساعت\n        self.call_history = {}\n    \n    def check_call(self, caller: str, receiver: str, duration: int) -> Dict:\n        \"\"\"بررسی تماس برای رفتار مشکوک\"\"\"\n        violations = []\n        risk_score = 0\n        \n        # بررسی مدت تماس\n        if duration > self.max_call_duration:\n            violations.append({\n                'type': 'تماس طولانی',\n                'duration': duration,\n                'risk_score': 10\n            })\n            risk_score += 10\n        \n        # بررسی تعداد تماس‌ها\n        if caller not in self.call_history:\n            self.call_history[caller] = []\n        \n        # پاک کردن تماس‌های قدیمی\n        now = datetime.now()\n        self.call_history[caller] = [\n            t for t in self.call_history[caller]\n            if (now - t).seconds < 3600\n        ]\n        \n        if len(self.call_history[caller]) >= self.max_calls_per_hour:\n            violations.append({\n                'type': 'تماس‌های متعدد',\n                'count': len(self.call_history[caller]),\n                'risk_score': 15\n            })\n            risk_score += 15\n        \n        self.call_history[caller].append(now)\n        \n        return {\n            'caller': caller,\n            'receiver': receiver,\n            'duration': duration,\n            'timestamp': now.isoformat(),\n            'has_violation': len(violations) > 0,\n            'violations': violations,\n            'risk_score': risk_score,\n            'action': 'محدود کردن' if risk_score >= 15 else 'قبول'\n        }\n\n\nclass SorushBehaviorAnalyzer:\n    \"\"\"تحلیل رفتار کاربران سروش\"\"\"\n    \n    def __init__(self):\n        self.user_activity = {}\n    \n    def analyze_user(self, username: str, activities: List[Dict]) -> Dict:\n        \"\"\"تحلیل رفتار کاربر\"\"\"\n        if username not in self.user_activity:\n            self.user_activity[username] = []\n        \n        self.user_activity[username].extend(activities)\n        \n        violations = []\n        total_risk = 0\n        \n        # بررسی فعالیت غیرمعمول\n        message_count = len([a for a in activities if a.get('type') == 'message'])\n        call_count = len([a for a in activities if a.get('type') == 'call'])\n        \n        if message_count > 100:  # بیش از 100 پیام در یک ساعت\n            violations.append({\n                'type': 'spam_flood',\n                'count': message_count,\n                'risk_score': 20\n            })\n            total_risk += 20\n        \n        if call_count > 30:  # بیش از 30 تماس در یک ساعت\n            violations.append({\n                'type': 'call_flood',\n                'count': call_count,\n                'risk_score': 25\n            })\n            total_risk += 25\n        \n        return {\n            'username': username,\n            'activity_count': len(activities),\n            'message_count': message_count,\n            'call_count': call_count,\n            'violations': violations,\n            'total_risk': total_risk,\n            'status': 'مشکوک' if total_risk >= 20 else 'عادی',\n            'recommendation': self._get_recommendation(total_risk)\n        }\n    \n    def _get_recommendation(self, risk: int) -> str:\n        \"\"\"توصیه برای اقدام\"\"\"\n        if risk == 0:\n            return 'بدون اقدام'\n        elif risk <= 20:\n            return 'نظارت'\n        elif risk <= 50:\n            return 'هشدار'\n        else:\n            return 'بن موقت یا دائم'\n\n\ndef test_filter():\n    \"\"\"تست فیلتر\"\"\"\n    print(\"🧪 تست سیستم فیلتری سروش\\n\")\n    print(\"=\" * 60)\n    \n    filter_system = SorushMessageFilter()\n    \n    # تست پیام‌های مختلف\n    test_messages = [\n        (\"سلام، چطور می‌تونم کمکت کنم؟\", \"user1\"),\n        (\"گاییدم، احمق!\", \"user2\"),\n        (\"میکشم تو رو!!!!!!\", \"user3\"),\n        (\"رمزت رو بفرست\", \"user4\"),\n        (\"خریدید محصولات با تخفیف\", \"user5\"),\n    ]\n    \n    for message, username in test_messages:\n        result = filter_system.check_message(message, username)\n        print(f\"\\n👤 کاربر: {username}\")\n        print(f\"💬 پیام: {message}\")\n        print(f\"📊 امتیاز خطر: {result['total_risk']}\")\n        print(f\"⚠️  سطح: {result['risk_level']}\")\n        print(f\"🎯 عمل: {result['action']}\")\n        print(f\"🔒 بن: {'بله' if result['is_banned'] else 'خیر'}\")\n        \n        if result['violations']:\n            print(f\"❌ تخلفات:\")\n            for v in result['violations']:\n                print(f\"   - {v.get('type', v.get('pattern'))}: {v.get('word', '')} (+{v['risk_score']})\")\n    \n    print(\"\\n\" + \"=\" * 60)\n    print(\"✅ تست کامل شد!\")\n\nif __name__ == '__main__':\n    test_filter()\n